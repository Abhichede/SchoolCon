$('#division').html("<%= escape_javascript options_for_select(@divisions.map {|division| [division.name, division.id]}.insert(0, 'Division')) %>");
$('#subject').html("<%= escape_javascript options_for_select(@subjects.map {|subject| [subject.name, subject.id]}.insert(0, 'Subject')) %>");
<%- model_class = ClassTeacher -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:division_id) %></th>
      <th><%= model_class.human_attribute_name(:teacher_id) %></th>
      <th><%= model_class.human_attribute_name(:standard_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @class_teachers.each do |class_teacher| %>
      <tr>
        <td><%= link_to class_teacher.id, class_teacher_path(class_teacher) %></td>
        <td><%= class_teacher.division_id %></td>
        <td><%= class_teacher.teacher_id %></td>
        <td><%= class_teacher.standard_id %></td>
        <td><%=l class_teacher.created_at %></td>
        <td>
          <%= link_to t('.show', :default => t("helpers.links.show")),
                      class_teacher_path(class_teacher), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_class_teacher_path(class_teacher), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      class_teacher_path(class_teacher),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_class_teacher_path,
            :class => 'btn btn-primary' %>

<%= form_with(model: parent_message, local: true) do |form| %>
  <% if parent_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parent_message.errors.count, "error") %> prohibited this parent_message from being saved:</h2>

      <ul>
      <% parent_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.number_field :student_id, id: :parent_message_student_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.number_field :teacher_id, id: :parent_message_teacher_id %>
  </div>

  <div class="field">
    <%= form.label :message %>
    <%= form.text_area :message, id: :parent_message_message %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

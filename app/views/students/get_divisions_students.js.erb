<% if @filtered_divisions  %>
  $('#division').html("<%= escape_javascript options_for_select(@divisions.map {|division| [division.name, division.id]}) %>");
<% end %>

<% if !@student.nil? %>
  $('#division').val(<%= @student.division.id %>);
<% end %>
<% if @filtered_divisions  %>
$('#division').html("<%= escape_javascript options_for_select(@divisions.map {|division| [division.name, division.id]}.insert(0, '')) %>");
<% end %>

<% if !@attendance.nil? %>
  $('#division').val(<%= @attendance.division.id %>);
<% end %>